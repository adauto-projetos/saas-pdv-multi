---
title: Code Quality Review — SAAS PDV.multi
generated_by: code-quality-analyzer
date: 2026-06-29
project: SAAS PDV.multi
version: v0.12.0
scope: app/ · components/ · lib/services/ · db/
method: evidence-based sampling of real code (path:line refs)
---

# Code Quality Review — SAAS PDV.multi

> Generated by code-quality-analyzer on 2026-06-29
> Maturing MVP+ (v0.12.0, ~20 features). Calibrated accordingly: this is well above typical MVP hygiene.

## TL;DR

The backend (services + actions + data + RLS) is genuinely strong: a uniform, well-documented pipeline (validate → auth → tenant guard → permission → service-under-RLS → revalidate → `toActionError`), function-based services with a clean service/data split, typed domain errors that never leak DB internals, and `any` essentially absent from production code (3 hits, all in tests). The 90 test files against 234 source files signal real coverage discipline.

The weak spots are all in the **UI layer**: heavy reliance on inline `style={{}}` objects (222 occurrences across 29 of 90 components) instead of the project's chosen Tailwind v4 convention, a few oversized client components (`CashierScreen.tsx` 871 lines) that mix presentation + state + data orchestration, and duplicated money-formatting/category-style helpers. None of these threaten correctness or the multi-tenancy guarantees; they are maintainability debt concentrated in `components/`.

## Executive Summary

| Area | Status | Score |
|------|--------|-------|
| SOLID | Good | 8/10 |
| Clean Code | Good | 8/10 |
| Architecture | Good | 9/10 |
| Technical Debt | Low–Medium | - |

## SOLID Analysis

### Single Responsibility
**Status:** Partially

**Findings (positive):**
- Services follow a strict per-domain split: each domain has `*-service.ts` (orchestration) and `*-data.ts` (queries). E.g. `lib/services/comanda/comanda-service.ts` orchestrates; `comanda-data.ts` does pure data access. Clean SRP at the module level.
- Domain error types are isolated in `lib/services/errors.ts` — one file, one concern.

**Findings (to improve):**
- `components/caixa/CashierScreen.tsx:46` — 871-line client component holds 7+ `useState`, search/filter `useMemo`, pre-quantity logic, category styling, stock badges, and submit orchestration. It is doing presentation + local state machine + action calls. → Extract `getCategoryStyle`/`getStockBadge` (CashierScreen.tsx:31-42) and the filter logic into hooks/helpers; split the product-grid and cart-tab concerns.
- `app/(app)/usuarios/UsuariosClient.tsx:1` (538 lines) and `components/caixa/PaymentDialog.tsx` (538 lines) carry several sub-components plus state in one file → split sub-components into siblings.

### Open/Closed
**Status:** Followed

**Findings:**
- No switch/if-chains on type discriminators were found in `lib/` (grep for `switch (`/`case "` over services: zero matches) — branching is data-driven (e.g. `SENSITIVE_ACTIONS[actionCode]` lookup in `override-service.ts:83`, `PERMISSION_PRESETS` maps).
- `lib/services/print/printer-driver.ts` is the only production class — a driver/strategy boundary, which is the right use of a class for extension.

### Liskov Substitution
**Status:** N/A (followed where applicable)

- The only inheritance hierarchy is the error tree in `lib/services/errors.ts:13-51` (`AppError` → `ValidationError`/`ConflictError`/`NotFoundError`/`UnauthorizedError`). Subclasses extend without weakening the base contract; `toActionError` (errors.ts:90) narrows by `instanceof` in most-specific-first order. Sound.

### Interface Segregation
**Status:** Followed

- Service functions take narrow, purpose-built inputs (`AuthContext` + a validated zod input type) rather than god-objects. `RunWithOverrideOptions<T>` (`override-service.ts:31`) is a small, focused options interface.

### Dependency Inversion
**Status:** Partially

**Findings:**
- There is no IoC/DI container, and services import concrete `*-data` modules directly (e.g. `comanda-service.ts:25` `import * as data from "./comanda-data"`). For a function-based Next.js app this is a pragmatic, acceptable choice — not classic DI, but the seams are clean and mockable in tests (90 test files confirm this works in practice).
- The DB seam is abstracted well: everything routes through `withUserRls` (`db/rls.ts:25`), so the RLS/transaction concern is inverted out of every service. Strong.
- Note: no `IPlanService`-style interfaces exist yet (grep `export interface I[A-Z]` in `lib/` → zero). Fine for current scale; revisit if a second adapter (e.g. billing provider) appears.

## Clean Code Analysis

### Naming Conventions
**Status:** Consistent

- Files match CLAUDE.md rules: app components PascalCase (`TenantTable.tsx`, `CashierScreen.tsx`), `ui/` primitives kebab-case (`help-tip.tsx`, `use-cart.ts`), services kebab-case. snake_case in DB schema. Functions/identifiers camelCase/PascalCase throughout. No cryptic abbreviations found in sampled files.

### Function Size
**Status:** Mostly good

Service functions are generally tight and single-purpose. The few large ones are inherent transactional orchestrators with step comments:

| File | Function | ~Lines | Note |
|------|----------|-------|------|
| `lib/services/comanda/comanda-service.ts:227` | `closeComanda` | ~120 | Mirrors `finalizeSale`; numbered steps + RN refs. Cohesive but long — the per-item snapshot loop (254-279) could extract a `buildSaleItemRows` helper. |
| `components/caixa/CashierScreen.tsx:46` | `CashierScreen` | component body | See SRP above. |

### Code Comments
**Status:** Appropriate (a notable strength)

- Comments explain **why**, tied to business rules (RN/RF refs), not what. `db/rls.ts:7-24` documents the RLS rationale and the founder double-check; `override-service.ts:71-77` documents the 3-path gate. This is exemplary domain documentation, not noise.

### Error Handling
**Status:** Followed (excellent)

- Single funnel: services throw typed `AppError` subclasses; actions wrap in `try/catch` and call `toActionError` (`lib/services/errors.ts:90`) which guarantees no stack/DB detail reaches the client. Verified across all 17 action files (18 files import `toActionError`).
- Side-effect isolation done right: in `closeComandaAction` (`app/(app)/comandas/actions.ts:163-186`) the post-commit print is wrapped so a printer failure NEVER turns a committed sale into `ok:false` — returns a `printWarning` instead. Mature handling of the success-with-warning case.
- `isUniqueViolation` (`errors.ts:72`) walks the Drizzle error `.cause` chain to detect SQLSTATE 23505 — robust, not a fragile string match.

## Architecture Patterns

### Layering
**Status:** Excellent — contract is enforced in practice

- Verified the inner-never-imports-outer rule holds: zero matches for `lib/services/**` importing `@/app/` or `@/components/`. The dependency direction (UI → actions → services → data) is real, not aspirational.
- Multi-tenancy discipline is uniform: every service operation is wrapped in `withUserRls(ctx.userId, ...)` and uses `ctx.tenantId`/`ctx.userId` rather than trusting client input. RLS is the documented last line of defense (`db/rls.ts`), and there is a dedicated regression suite (`db/__tests__/tenant-isolation-regression.test.ts`, 728 lines) that derives the expected tenant-scoped table set from the schema — protects against forgetting `tenant_id` on new tables.

### Cohesion & Coupling
**Status:** Good

- Domains are cohesive; cross-domain calls are explicit and minimal (e.g. comanda close reuses `recordSaleReceivable`, `insertCashMovement`, `selectOpenSessionId` — mirroring sale finalization rather than duplicating it).

## Technical Debt

### TODOs/FIXMEs Found
**Status:** Effectively none.

A grep for `TODO|FIXME|HACK|XXX` returns only false positives — Portuguese "TODOS" (= "all") in comments (`comanda-service.ts:163`, `apply-rls.ts:6`). No genuine debt markers in the codebase. Notable for a 20-feature MVP.

### TypeScript Strictness
**Status:** Strong. `strict: true` in `tsconfig.json:7`. Only 3 `: any` usages exist and all are in `db/__tests__/tenant-isolation-regression.test.ts` (lines 116, 619, 653) where reflecting over Drizzle internals justifies it. No `@ts-ignore`/`@ts-expect-error` anywhere.

### Code Duplication
**Areas identified:**
- **Inline styles vs Tailwind:** 222 `style={{}}` occurrences across 29 components. Hotspots: `CashierScreen.tsx` (57), `PaymentDialog.tsx` (22), `AppSidebar.tsx` (18), `help-tip.tsx` (14), `admin/TenantTable.tsx` (13), `Cart.tsx` (12). The project's stated UI convention is Tailwind v4 — these inline objects duplicate spacing/color tokens and diverge from the design system.
- **Category color / stock badge helpers:** `getCategoryStyle`/`getStockBadge` (`CashierScreen.tsx:31-42`) are presentation logic likely needed elsewhere (product lists) — candidate for a shared `lib/ui` or component-level extraction.
- **Money formatting:** `centsToBRL` is centralized (`lib/format/money`) and used broadly (good), but a grep also flags scattered usage; ensure no `toFixed(2)` ad-hoc formatting creeps in alongside it.

### Deprecated Code
- None found. The git status shows an in-progress rename of admin components to PascalCase (kebab → Pascal) and deletion of legacy `components/PDVApp.jsx/.css` — i.e. debt is actively being paid down, not accumulating.

## Improvement Opportunities

### High Priority
- [ ] **Decompose `CashierScreen.tsx`**: extract presentation helpers (`getCategoryStyle`, `getStockBadge`) and the product-filter `useMemo` into a `use-product-filter` hook; split product-grid vs cart-tab UI. | Impact: M | Effort: M
- [ ] **Adopt Tailwind for high-traffic components**: migrate inline `style={{}}` in `CashierScreen`, `PaymentDialog`, `Cart`, `AppSidebar` to Tailwind classes / design tokens, eliminating duplicated color/spacing literals. | Impact: M | Effort: M

### Medium Priority
- [ ] **Extract `buildSaleItemRows` from `closeComanda`** (`comanda-service.ts:254-279`) to shorten the orchestrator and share the snapshot loop with `finalizeSale`. | Impact: L | Effort: S
- [ ] **Split multi-component files** (`UsuariosClient.tsx`, `PaymentDialog.tsx`) into one component per file. | Impact: L | Effort: S

### Low Priority / Nice to Have
- [ ] Introduce a small `lib/ui` for shared presentation helpers (category palette, stock badge) to prevent future duplication.
- [ ] Consider a service-interface seam (e.g. `IPrintDriver`) only if/when a second print or billing adapter lands — not needed today.

## Recommendations

### Quick Wins (< 1 day)
1. Extract `getCategoryStyle`/`getStockBadge` out of `CashierScreen.tsx` into a shared module.
2. Extract `buildSaleItemRows` from `closeComanda`.
3. Split `PaymentDialog.tsx`/`UsuariosClient.tsx` sub-components into sibling files.

### Medium-term (1–5 days)
1. Migrate the top-5 inline-style components to Tailwind classes / a token layer; establish a lint guard discouraging new `style={{}}`.
2. Decompose `CashierScreen` state into focused hooks.

### Long-term (refactoring)
1. Establish a `components/ui` token/style convention doc so the design system stops drifting into inline styles, then sweep the remaining 24 components incrementally.

---
*Review generated automatically from sampled real code. Manual validation recommended. Backend/architecture quality is high; remediation focus is the UI layer's styling convention and a few oversized client components.*
